# Runtime dependencies — Python 3.14
#
# Direct dependencies only. Transitive deps (asgiref, sqlparse) are resolved
# by pip and intentionally not listed here.

Django==6.0.7
djangorestframework==3.17.1
django-filter==26.1
mysqlclient==2.2.8
python-dotenv==1.2.2
